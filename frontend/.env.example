# TunjiaX Frontend Environment Variables
# Copy this file to .env and fill in the values

# ⚠️ IMPORTANT FOR CLOUD RUN DEPLOYMENT:
# These VITE_ vars are BUILD-TIME only (baked into the JS bundle).
# When deploying with Docker, pass them as --build-arg:
#
#   docker build --build-arg VITE_GOOGLE_CLIENT_ID=xxx \
#                --build-arg VITE_BACKEND_URL=https://your-url \
#                --build-arg VITE_ELEVENLABS_AGENT_ID=xxx .
#
# Or with Cloud Build:
#   gcloud builds submit --substitutions=_VITE_GOOGLE_CLIENT_ID=xxx,...

# === Google OAuth ===
# Same value as backend GOOGLE_CLIENT_ID (Vite requires VITE_ prefix)
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id

# === Backend API ===
# Local: http://localhost:8080
# Production: https://your-cloud-run-url.run.app
VITE_BACKEND_URL=http://localhost:8080

# === ElevenLabs Voice Agent ===
VITE_ELEVENLABS_AGENT_ID=your_elevenlabs_agent_id
